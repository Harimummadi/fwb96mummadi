extends layout

block content
  h1= title
  - var images=["Giraffe.jpg","Giraffe1.jpg","Giraffe2.jpg","Giraffe3.jpg","Giraffe4.png"] 
  - for(i=0;i<3;i++)
    - let value = Math.floor(Math.random()*5)
        img(src=`images/${images[value]}`, height='300px', width='300px')
